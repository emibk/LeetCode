class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int xor_=nums[0];
        for(int i=1;i<nums.size();i++)
        {
            xor_=xor_ ^ nums[i];
        }
        return xor_;
    }
};
